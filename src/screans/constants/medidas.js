import { Dimensions } from "react-native";
// pasata contendo dados que eerão reutilizados na página inteira

export const width = Dimensions.get("window").width;
export const height = Dimensions.get("window").height;


